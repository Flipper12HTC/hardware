#include <Arduino.h>

void setup() {
  Serial.begin(115200);
  delay(100);
  Serial.println();
  Serial.println("flipper12-hardware — starting up...");
  Serial.println("Skeleton only. No logic yet.");
}

void loop() {
  
}